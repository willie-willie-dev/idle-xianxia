# 随手记 account-export 直链访问分析

> 抓取时间：2026-06-02
> 目标 URL：`https://www.feidee.com/cloud/#/account-export`

## 1. 页面是否需要登录

**是**

**依据：**

- 随手记 Web 为 SPA；`#/account-export` 路由对应受保护组件 `AccountExport`，未持会话 Cookie 时通常会重定向至 `/?needLogin=true` 或叠加登录层。
- 同站未登录 QA：顶栏显示橙色「**登录**」按钮（非头像/昵称）；点击「开始记账」等 CTA 会弹出登录模态框。
- 无头/静态抓取该 URL 仅得到营销壳页与 IE 兼容提示，未见导出表单字段，符合需鉴权后由前端渲染的模式。

## 2. 是否直接显示了导出界面

**否**

**实际展示（未登录新会话直链）：**

- 登录页、登录弹窗，或带 `needLogin=true` 的首页/壳层。
- **未**直接出现「选择账本 / 日期范围 / 导出格式（CSV/Excel）」等导出 UI；导出文案需登录并进入账本上下文后才可见。

## 3. 登录方式与步骤

### 扫码登录（默认 Tab）

1. 打开页面后若出现登录弹窗，确认在「**扫码登录**」Tab。
2. 使用**随手记 App** 扫描页面二维码并在手机端确认。
3. Web 端会话建立后，再访问 `#/account-export` 或从账本 **设置 → 导出** 进入。

### 账号密码登录

1. 切换到「**账号登录**」Tab。
2. 输入账号与密码并登录。
3. **我的账本** → 选择账本 → **设置** → **导出**，或直链 `#/account-export`。

### 手机号 / 验证码登录

1. 切换到「**手机号登录**」Tab。
2. 输入手机号，填写短信验证码并登录。
3. 同上进入导出功能。

### 其他

- 顶栏橙色「**登录**」可主动打开登录弹窗。
- 弹窗底部可能有「我是新用户」「切换旧版登录界面 >」。

---

*分析依据：`tmp-qa/sui-web-analysis.md` 路由逆向 + 同域未登录截图 QA；请运行 `tmp-qa-capture.mjs` 生成 `sui-export-direct.png` 做视觉复核。*
